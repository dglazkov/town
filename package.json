{
  "name": "town",
  "version": "0.0.0",
  "private": true,
  "description": "A capability system for agents: shops handed to an agent as a CLI.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "town": "bin/town.js",
    "townd": "bin/townd.js"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.build.json && vitest run",
    "typecheck": "tsc -p tsconfig.json && mkdir -p .wrangler/types && wrangler types .wrangler/types/worker.d.ts --include-env=false > /dev/null && tsc -p tsconfig.worker.json"
  },
  "dependencies": {
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^24.13.4",
    "typescript": "^7.0.2",
    "vitest": "~4.1.11",
    "wrangler": "^4.131.1"
  }
}
