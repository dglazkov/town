# Projects

One directory per body of work. A project holds everything about itself:
the ideal it is aiming at (`journey.md`), the mechanism (`design.md`), and
the walk that gets there (`phases.md`). Reading a project end to end is
`ls` and then reading in order; adding a doc to one edits nothing outside
it.

**Where each project stands lives in its own primary doc**, in front
matter, so this table cannot be more right than the thing it describes.

**Each phase has a `**Status:**` line, and its word is one of four:**
`NOT STARTED`, `PART-DONE`, `CLOSED`, or `WITHDRAWN`, followed by the date
it last moved and one sentence of what holds. A newly planned project's
phases are all `**Status: NOT STARTED.**`; never `OPEN`, which the
conductor's status script does not know. The words are defined in
[`/conduct`](../../.claude/skills/conduct/SKILL.md), and
`.claude/skills/conduct/status.sh <project>` lints them.

**A project is short.** It holds one body of work with an end: a journey
that can be walked, phases that close, a last phase after which nothing
in it waits on work. A project that turns out to be long-lived was cut at
the wrong grain and should have been several. The pilot in the draft
([../drafts/town-design-doc.md](../drafts/town-design-doc.md) §16) is
several such projects, each named for what it adds to the town.

| Project | What it is | Where it stands |
| --- | --- | --- |
| [gate](gate/) | The town's gate: where a grant is checked. One server on one box, a grant file in an agent's directory, the `town` command as a thin client and `townd` as the operator's, help rendered on the server for the calling grant, a call allowed or denied before any shop runs, and one shop, `town/memory`, built from a manifest and run as a subprocess. No credentials, no Square, no Town Hall: the operator puts shops and grants in at the box. [`design.md`](gate/design.md) is the argument, [`journey.md`](gate/journey.md) the acceptance suite, [`phases.md`](gate/phases.md) the walk. | **Done 12 Sep 2026.** All three phases closed. The town runs on one laptop: manifest, runtime, `town/memory`, the store, the gate, help for the grant, the audit, the admin, and `town` as a pipe. It was walked by a real Claude Code session: no call denied, and after its grant was narrowed it said it could no longer write memory. Nothing waits. |
| [vault](vault/) | The town's vault: where a credential is kept and used on a shop's behalf without the shop ever holding it. Typed credentials seeded with a GitHub token, the operator connecting one from stdin, rows sealed under a key file, a binding made with the grant, and the teller: a loopback window opened per call that signs a shop's requests to the type's origin and closes after. One new shop, `town/github`. No OAuth, no Square, no egress control. [`design.md`](vault/design.md) is the argument, [`journey.md`](vault/journey.md) the acceptance suite, [`phases.md`](vault/phases.md) the walk. | **Done 12 Sep 2026.** All three phases closed. Credentials are sealed under `vault.key`, bound to grants, opened only when a call is allowed, and signed onto the wire by a per-call teller. `town/github` was walked by a real Claude Code session on a real token: no call denied, the reply on GitHub, and after narrowing it said it could no longer reply; the token was in no file searched. Open, owed to a containers project: on one box a shop can read the key and unseal. |
| [compose](compose/) | The town's compose: where a shop calls other shops through the town as the agent that called it, and never as more. A manifest's `depends` naming shops and commands, a clerk opened per call that answers a shop's own `town` calls with a grant cut from the agent's, liveness that needs the dependencies held, the audit as a call tree, and a denial one level down told to the agent in the same words. One new shop, `town/watch`, a recipe over `town/github` and `town/memory`. No private needs, no Town Hall, no containers. [`design.md`](compose/design.md) is the argument, [`journey.md`](compose/journey.md) the acceptance suite, [`phases.md`](compose/phases.md) the walk. | **Done 12 Sep 2026.** All three phases closed. A shop calls other shops through `town` with the agent's grant cut by its manifest at every depth, liveness needs the dependencies held, a denial one level down reaches the agent in the dependency's words, and the audit is a call tree. `town/watch` was walked by a real Claude Code session on a real token: the closed issue reported, the unnamed repository declined, watch gone when memory narrowed, and the token in no file searched. Nothing waits. |
| [hall](hall/) | The town's hall: where an agent finds, writes, publishes, and asks for shops. Town Hall as `town/hall`, a shop the town is born with whose commands run in the town's process, granted and narrowed like any other; a shop sent on stdin as a tar, validated against the spec, tested with the agent's own grants, kept under the user's name, and held by the publishing pass alone; and a permit, a grant an agent proposes and the operator approves, as asked or narrower, or denies. No second user, no credentials at a sent shop, no Square, no containers. [`design.md`](hall/design.md) is the argument, [`journey.md`](hall/journey.md) the acceptance suite, [`phases.md`](hall/phases.md) the walk. | **Done 12 Sep 2026.** A real Claude Code session read the hall's help and spec, wrote a to-do shop, got it through `validate` on the third try (both refusals §2), published it, and used it. Asked for what its grant did not allow, it asked with `request`, and did it once the operator approved. The token was in no file searched but the grant file. Next is [wall](wall/). |
| [wall](wall/) | The town's wall: where a shop's process is enclosed. The runtime contract of spec §7 made a mechanism: a shop reads its own directory, reads and writes its state, reaches the tellers and the clerk the town opened for the call, and nothing else on the box, not the data directory, not the key, not the operator's home, not the network, not the town itself. Seatbelt on this Mac behind a seam the Linux box will fill, `--wall none` to run unwalled and say so, the audit's `wall` column, and vault's exfil test turned around. The project earlier docs call containers. [`design.md`](wall/design.md) is the argument, [`journey.md`](wall/journey.md) the acceptance suite, [`phases.md`](wall/phases.md) the walk. | **Done 12 Sep 2026.** `townd serve` walls every shop by Seatbelt on this Mac unless `--wall none` is written, and refuses a box without a wall; the audit has a `wall` column, and vault's exfil test reads `EPERM` for the key. A real Claude Code session wrote a shop that pried, published it, and ran it walled: its report named its own directory and state readable and the town's files, the home, and the network refused, and asked for a page's title, it said its shops cannot reach the web. The token was in no file searched but the grant file. Next is [consent](consent/). |
| [consent](consent/) | The town's consent: where a credential the town has never seen enters it. A manifest's need may define its type, origin, header, and for `oauth` the endpoints and scopes, so an agent proposes a type with its shop; a person reads the origin in the permit and approves the type at the box; a credential is pasted once or consented to in a browser with the redirect caught on loopback by `townd admin credential connect`; the grant that binds it is a permit, never a side effect, with the shop's tests run at approval; the person is told where the secret is made in the agent's words, a need's `guidance`, and what to type by `permit show`'s checklist; and the gate refreshes an `oauth` credential before a call. One new shop, `town/gdocs`. No Square, no person off the box, no registration from a manifest. [`design.md`](consent/design.md) is the argument, [`journey.md`](consent/journey.md) the acceptance suite, [`phases.md`](consent/phases.md) the walk. | **Done 13 Sep 2026.** All four phases closed. A real Claude Code session proposed a Figma type in its manifest; a person with no Figma account made the token from the agent's guidance, `permit show`'s checklist reached a grant with the shop's tests run at approval, and the agent read a file's comments and, through `town/gdocs` on a real Google consent, a document's first line; no secret in any file searched. The walk's two holes closed in consent phase 3: a proposer's republish rewrites its type's guidance, and `credential add --replace`, offered by the checklist, moves grants to a new secret. Nothing waits. |
| [box](box/) | The town's box: the town off the laptop as one Cloudflare Worker. The store in a Durable Object's own SQLite behind a `Sql` seam, a shop's files as rows behind a shelf, the vault's key a secret; `runtime: worker` beside `runtime: subprocess`, a shop whose program is a function, run as a process on a laptop and in an isolate on the box, and the four shops made worker shops; the isolate's only way out the window the town made for the call, so the wall is the absence of a binding; the operator's verbs over the wire with a token of their own; a consent landing at the town's address; `pnpm box deploy`; and `town` unchanged. No process on the box, no second person, no Linux wall. [`design.md`](box/design.md) is the argument, [`journey.md`](box/journey.md) the acceptance suite, [`phases.md`](box/phases.md) the walk, and [`../spikes/box/`](../spikes/box/README.md) what was measured first. | **Done 13 Sep 2026.** All five phases closed. The town runs on the operator's Cloudflare account as `https://town.dglazkov.workers.dev`: the store, the shops' files, and their state in a Durable Object, a worker shop in an isolate whose only way out is its window, the operator's verbs over the wire, and a Google consent landing at the box. A real Claude Code session wrote a worker shop from spec §7 unprompted, published and ran it on the box, asked for `town/gdocs` by a permit approved over the wire and read a document, and said its shops cannot reach the web; the platform's CPU limit ended a spinning shop. Nothing waits; the sheep walk is next. |
| [baton](baton/) | The conductor's tools: a brief written by a script from a project's docs in the conduct skill's shape, a mutation check that backs up, edits, runs, and restores by copy so a dirty tree loses nothing, and `pnpm test` grown to take a ring, a file, or `--list`, building only when `dist/` is stale. Nothing a shop, an agent, or an operator can see changes; the project conducts its own last two phases with its first tool. The first star cut from the [infra constellation](../drafts/infra-constellation.md) and the first project of the [night sky](../drafts/night-sky.md). [`design.md`](baton/design.md) is the argument, [`journey.md`](baton/journey.md) the acceptance suite, [`phases.md`](baton/phases.md) the walk. | **Done 13 Sep 2026.** Baton phases 0 to 2 closed: `brief.sh` writes a phase's brief from the docs, `pnpm test` takes a ring, a file, `--list`, or `--watch`, and `mutate.mjs` checks a mutation by copy in a dirty tree. Two Opens, both test gaps; nothing waits on a person. |
| [road](road/) | The town's road: where the wire the agent's binary speaks becomes a contract any harness may implement. `TOWN_GRANT` carries the grant itself as well as the path of a file holding one, the refusal of a bad value printing none of it; `docs/harness.md` says in numbered sections what a `town` posts, prints, and refuses; and `scripts/conform.mjs` proves a harness against a town it sets up, on a laptop or over the box, a line per check citing the contract. One check shop, `test/conform`. No sheep, no version on the wire, no install path. The second project of the [night sky](../drafts/night-sky.md), cut from the [sheep constellation](../drafts/sheep-constellation.md). [`design.md`](road/design.md) is the argument, [`journey.md`](road/journey.md) the acceptance suite, [`phases.md`](road/phases.md) the walk. | **Done 14 Sep 2026.** All three phases closed. `TOWN_GRANT` carries the grant itself or a path, refused without printing it when it holds no grant; [`docs/harness.md`](../harness.md) is the wire in ten sections and `scripts/conform.mjs` its thirty checks. An agent given the doc alone wrote a Python `town` that was conformant at its first run, on the laptop and on the operator's box, and the laptop binary was conformant on the box twice. Nothing waits; drove is next. |
| [tent](tent/) | The town's tent: the ladder's top two rungs as one script. `pnpm hermetic --ring account` pitches a box named for the commit under a HOME of the ring's own, runs road's conformance over the wire, and strikes it, the account's listing the same before and after; `pnpm hermetic --ring agent --sheep <dir>` furnishes that box with a user, a credential from stdin, and the memory and github shops, and runs drove's stage against it as a program, a real sheep on the standing station, its exit the verdict; `--dry-run`, `--keep`, `--strike`, `--list`. The ring chooses the environment, never the steps, and reads no file of sheep's. No package or machine ring, no consent from afar, no CI. The stars tent and stranger of the [infra constellation](../drafts/infra-constellation.md) cut together, the fourth row of the [night sky](../drafts/night-sky.md) and the first project past the line. [`design.md`](tent/design.md) is the argument, [`journey.md`](tent/journey.md) the acceptance suite, [`phases.md`](tent/phases.md) the walk. | **Partial 14 Sep 2026.** Tent phase 0 closed: `pnpm hermetic --ring account` walked on the account, green twice at thirty `ok`, kept and struck, failing on purpose and struck, the listing the same; box's deploy now waits for its door. Tent phase 1 is next: the stranger, its ⚑ steps answered. |
